for cloning the git repository/document
...
git clone url

git add . //adding all the files from local repo to remote repo

git commit -m "text message" //cloning github repo to local computer

git push -u origin main //push rhe code to github

git checkout -b brach_name //creating a new branch
